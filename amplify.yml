version: 1

frontend:
  phases:
    build:
      commands:
        - echo "Building MCU Movies Explorer..."
  artifacts:
    baseDirectory: /
    files:
      - mcu-movies.html
      - index.html
  cache:
    paths: []
